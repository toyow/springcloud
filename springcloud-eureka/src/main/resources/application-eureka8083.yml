server:
  port: 8083
eureka:
  instance:
    hostname: eureka8083
  server:
    enable-self-preservation: false
  client:
    register-with-eureka: false
    fetch-registry: false
    #    service-url.defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    service-url:
      defaultZone: http://eureka8082:8082/eureka/
